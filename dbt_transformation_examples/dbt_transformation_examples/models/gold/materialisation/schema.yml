version: 2

# Group 8 — Materialisation strategies. dbt-specific build patterns rather than
# pure SQL transformations. Each model is self-documented in-SQL; this file
# registers the models for parse/test only.

models:
  - name: incremental_daily_metrics
    description: "Incremental model: MERGE only new/changed source rows via is_incremental()."
